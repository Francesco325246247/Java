
int main(void) {

	int a[100];
	int max;
	int min;

	//counter of the array list


		 //check the element inside the array
		 for (int j = a[j]; j < (a[100] - 1); j++) {

			 int temp = j;
			        	 if (a[temp] < (a[j]+1)){temp = min;}
			        	 else (a[temp] > (a[j]+1)){temp = max;}

			        	 for (int i = 0; i <= a[100]; i++)
			        	    {
			        		 	if (i == a[max]){
			        		 		printf("The maximum element in position [i] = %d\n", i, max );}
			        		 	else if (i == a[min])
			        		 		printf("The minimum element in position [i] = %d\n", i, min );
	    }

		 }
	return 0;
}
